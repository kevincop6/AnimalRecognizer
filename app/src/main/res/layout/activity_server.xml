<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1F2933"
    tools:context=".ServerActivity">

  <!-- ===================================================
       ÍCONO PRINCIPAL
       Usa ic_menu_upload de los drawables del sistema
       Android lo incluye por defecto, tinte verde Material
  =================================================== -->
  <ImageView
      android:id="@+id/ivServerIcon"
      android:layout_width="64dp"
      android:layout_height="64dp"
      android:src="@android:drawable/ic_menu_upload"
      android:contentDescription="@string/servidor"
      app:tint="#4CAF50"
      app:layout_constraintTop_toTopOf="parent"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintBottom_toTopOf="@id/tvTitle"
      android:layout_marginBottom="12dp"
      app:layout_constraintVertical_chainStyle="packed"
      app:layout_constraintVertical_bias="0.35" />

  <!-- ===================================================
       TÍTULO PRINCIPAL
       textAppearance nativo de Material Design
  =================================================== -->
  <TextView
      android:id="@+id/tvTitle"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="@string/servidor"
      android:textColor="#FFFFFF"
      android:textSize="26sp"
      android:textStyle="bold"
      app:layout_constraintTop_toBottomOf="@id/ivServerIcon"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintBottom_toTopOf="@id/tvSubtitle" />

  <!-- ===================================================
       SUBTÍTULO
       Color gris verdoso suave para jerarquía visual
  =================================================== -->
  <TextView
      android:id="@+id/tvSubtitle"
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:text="@string/configura_la_url_base_de_la_api"
      android:textColor="#7A8C7D"
      android:textSize="13sp"
      android:textAlignment="center"
      android:layout_marginTop="4dp"
      android:layout_marginStart="40dp"
      android:layout_marginEnd="40dp"
      app:layout_constraintTop_toBottomOf="@id/tvTitle"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintBottom_toTopOf="@id/tilServerUrl" />

  <!-- ===================================================
       CAMPO URL — TextInputLayout OutlinedBox
       • Sin fuentes externas
       • Colores hardcodeados directamente en hex
       • endIconMode="clear_text" es nativo de Material
       • boxStrokeColor con selector o color directo
  =================================================== -->
  <com.google.android.material.textfield.TextInputLayout
      android:id="@+id/tilServerUrl"
      style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:layout_marginStart="28dp"
      android:layout_marginEnd="28dp"
      android:layout_marginTop="28dp"
      app:boxStrokeColor="#4CAF50"
      app:boxBackgroundColor="#1C231E"
      app:boxStrokeWidth="1.5dp"
      app:boxCornerRadiusTopStart="14dp"
      app:boxCornerRadiusTopEnd="14dp"
      app:boxCornerRadiusBottomStart="14dp"
      app:boxCornerRadiusBottomEnd="14dp"
      app:hintTextColor="#4CAF50"
      app:endIconMode="clear_text"
      app:endIconTint="#7A8C7D"
      android:hint="@string/http_192_168_1_1_80_api"
      app:layout_constraintTop_toBottomOf="@id/tvSubtitle"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintBottom_toTopOf="@id/btnSave">

    <com.google.android.material.textfield.TextInputEditText
        android:id="@+id/etServerUrl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textUri"
        android:textColor="#FFFFFF"
        android:textColorHint="#7A8C7D"
        android:textSize="14sp"
        android:imeOptions="actionDone"
        android:singleLine="true" />

  </com.google.android.material.textfield.TextInputLayout>

  <!-- ===================================================
       BOTÓN GUARDAR
       backgroundTint hardcodeado en hex verde selva
       Sin íconos externos — solo texto limpio
  =================================================== -->
  <com.google.android.material.button.MaterialButton
      android:id="@+id/btnSave"
      style="@style/Widget.MaterialComponents.Button"
      android:layout_width="0dp"
      android:layout_height="52dp"
      android:text="@string/guardar"
      android:textColor="#FFFFFF"
      android:textSize="15sp"
      android:textStyle="bold"
      app:backgroundTint="#4CAF50"
      app:cornerRadius="16dp"
      android:layout_marginStart="28dp"
      android:layout_marginEnd="28dp"
      android:layout_marginTop="14dp"
      app:layout_constraintTop_toBottomOf="@id/tilServerUrl"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintVertical_bias="0.0" />

</androidx.constraintlayout.widget.ConstraintLayout>

